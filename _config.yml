# Book settings
# Learn more at https://jupyterbook.org/customize/config.html

title: X-Ray Reflectivity on SIRIUS
author: Arnaud HEMMERLE (Synchrotron SOLEIL)
copyright: "2024" # Copyright year to be placed in the footer
logo: _static/resized-logo-SIRIUS.jpg
# Patterns to skip when building the book. Can be glob-style (e.g. "*skip.ipynb")
exclude_patterns: [_build, Thumbs.db, .DS_Store, "**.ipynb_checkpoints"]
# Auto-exclude files not in the toc
only_build_toc_files: true

# Force re-execution of notebooks on each build.
# See https://jupyterbook.org/content/execute.html
execute:
  execute_notebooks: force

# Auto-exclude files not in the toc
only_build_toc_files: true

# Define the name of the latex output file for PDF builds
latex:
  latex_documents:
    targetname: book.tex

# Add a bibtex file so that we can create citations
bibtex_bibfiles:
  - chapters/appendices/references.bib

# Information about where the book exists on the web
repository:
  url: https://github.com/ArnaudHemmerle/xrr_on_sirius  # Online location of your book
  branch: master  # Which branch of the repository should be used when creating links (optional)

# Add GitHub buttons to your book
# See https://jupyterbook.org/customize/config.html#add-a-link-to-your-repository
html:
  use_issues_button: true
  use_repository_button: true
  home_page_in_navbar: false  # Whether to include your home page in the left Navigation Bar
